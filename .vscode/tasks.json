{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "rpi_py_debug",
            "type": "shell",
            "command": "bash ${workspaceRoot}/debug_remote.sh ${relativeFile}"
        },
        {
            "label" : "rpi_py_kill",
            "type" : "shell",
            "command" : "bash ${workspaceRoot}/kill_remote.sh"
        }
    ]
}